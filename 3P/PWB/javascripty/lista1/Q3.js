// =============================================================================================================================================================
// Questão 3
function tabuada(num) {
    if (num >= 0) {
        for (let i = 1; i <= 10; i++) {
            let result = i * num;
            console.log(`${i} x ${num} = ${result}`);
        }
    }
}
let num = 2;
tabuada(num)